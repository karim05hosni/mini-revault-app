export enum TransactionType {
  deposit = 'deposit',
  withdrawal = 'withdrawal',
  transfer = 'transfer',
  conversion = 'conversion',
}

export enum TransactionStatus {
  completed = 'completed',
  failed = 'failed',
}
