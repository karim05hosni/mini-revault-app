server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA routing
    location / {
        try_files $uri /index.html;
    }

    # Optional: forward API calls
    # location /api/ {
    #     proxy_pass http://backend:PORT/;
    # }
}